#include<iostream.h>
#include<conio.h>
int main()
{ 
  struct node //declaration of linked list
  {
  int a; //insertion of data 
  struct node *next; //pointer to store address of the next address
  }
}
